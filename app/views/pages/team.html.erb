<div class="container-m text-center">
  <div class="text-center mb-5"><h1>Meet The Team</h1></div>
  <h2>The Presidents</h2>
    <div class="grid-team two">
      <%= render "team_member" %>
      <%= render "team_member" %>
    </div>
  <h2>The Board</h2>
    <div class="grid-team">
      <%= render "team_member" %>
      <%= render "team_member" %>
      <%= render "team_member" %>
      <%= render "team_member" %>
    </div>
  <h2>The Advisory Board</h2>
    <div class="grid-team">
      <%= render "team_member" %>
      <%= render "team_member" %>
      <%= render "team_member" %>
    </div>
  <h2>The Crew</h2>
  <div class="grid-team">
    <%= render "team_member" %>
    <%= render "team_member" %>
    <%= render "team_member" %>
    <%= render "team_member" %>
    <%= render "team_member" %>
    <%= render "team_member" %>
  </div>
  <h2>Get in touch</h2>
  <p>Octagon is a student-led organisation that aims to empower young people to make a difference in their communities. We are always looking for new members to join our team, so if you are interested in getting involved, please get in touch!</p>
  <p>Send us your CV to <a href="mailto:info@octagon-global.com" class="link">info@octagon-global.com</a></p>
</div>
